# Index
DOM = structure of the HTML file


# How the website is built
We keep the html file the bare minimum and "outsource" everything to the code.

Everything in line frontend/index.html#L12 will be on out website.
What we do is we add content to this "div" element.
And this is done in the "mount" function in vdom.ts: frontend/src/lib/vdom/vdom.ts#L155

