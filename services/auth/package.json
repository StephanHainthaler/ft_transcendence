{
  "name": "@ft_transcendence/auth",
  "scripts": {
    "build": "tsc",
    "predev": "mkdir db || true",
    "dev": "tsx watch --env-file=../../env/.env.auth src/index.ts",
    "predev": "mkdir -p db",
    "test": "tsx --test --test-reporter=spec ./tests/*.test.ts"
  },
  "dependencies": {
    "@types/node": "^24.10.0",
    "argon2": "^0.44.0",
    "fastify": "^5.6.1",
    "qrcode": "^1.5.4",
    "speakeasy": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
